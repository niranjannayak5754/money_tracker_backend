openapi: 3.0.3
info:
  title: Money Tracker API
  description: Personal finance tracker backend with categories, income, expenses, summary reporting.
  version: "1.0.0"

servers:
  - url: /

tags:
  - name: health
    description: Health and diagnostics
  - name: auth
    description: Authentication and account operations
  - name: profile
    description: Profile-related endpoints for the authenticated user
  - name: category
    description: Category management (income/expense categories)
  - name: income
    description: Income entries
  - name: expenses
    description: Expense entries
  - name: summary
    description: Reporting and summaries

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

paths:
  /health:
    get:
      tags: [health]
      summary: Health check
      responses:
        '200':
          description: API is healthy

  /auth/register:
    post:
      tags: [auth]
      summary: Register new user
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password]
              properties:
                email: { type: string, format: email }
                password: { type: string, minLength: 6 }
      responses:
        '201':
          description: User created

  /auth/login:
    post:
      tags: [auth]
      summary: Login user and get JWT access token
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password]
              properties:
                email: { type: string }
                password: { type: string }
      responses:
        '200':
          description: Token response
          content:
            application/json:
              schema:
                type: object
                properties:
                  access_token:
                    type: string

  /auth/me:
    get:
      tags: [profile]
      summary: Fetch authenticated user's profile
      security:
        - bearerAuth: []
      responses:
        '200':
          description: User info returned

  /auth/logout:
    post:
      tags: [auth]
      summary: Logout user (invalidate token)
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Logged out

  /auth/reset-password:
    post:
      tags: [profile]
      summary: reset authenticated user's password
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [new_password]
              properties:
                new_password: { type: string, minLength: 6 }
      responses:
        '200':
          description: Password changed successfully

  /categories:
    get:
      tags: [category]
      summary: List categories
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: archived
          required: false
          schema:
            type: boolean
          description: |
            Filter categories by archived status.
            - `true`  → archived categories only
            - `false` → active categories only
            - omitted → all categories
      responses:
        '200':
          description: OK
    post:
      tags: [category]
      summary: Create category
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: { type: string }
                type:
                  type: string
                  enum: [expense, income]
      responses:
        '201':
          description: Category created

  /categories/{id}:
    put:
      tags: [category]
      summary: Update category
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                archived: { type: boolean }
      responses:
        '200':
          description: Updated

  /income:
    get:
      tags: [income]
      summary: List income entries for month
      security:
        - bearerAuth: []
      parameters:
        - name: month
          in: query
          schema: { type: string, example: "2025-01" }
      responses:
        '200': { description: OK }
    post:
      tags: [income]
      summary: Add income entry
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [amount]
              properties:
                amount: { type: number }
                date: { type: string, format: date-time }
                source: { type: string }
                notes: { type: string }
      responses:
        '201': { description: Created }

  /expenses:
    get:
      tags: [expenses]
      summary: List expenses for month
      security:
        - bearerAuth: []
      parameters:
        - name: month
          in: query
          schema: { type: string }
        - name: category
          in: query
          schema: { type: string }
      responses: { '200': { description: OK } }

    post:
      tags: [expenses]
      summary: Add expense entry
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [amount, category_id]
              properties:
                amount: { type: number }
                category_id: { type: string }
                date: { type: string, format: date-time }
                merchant: { type: string }
                notes: { type: string }
                tags:
                  type: array
                  items: { type: string }
      responses:
        '201': { description: Created }

  /expenses/{id}:
    put:
      tags: [expenses]
      summary: Update expense
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200': { description: Updated }
    delete:
      tags: [expenses]
      summary: Delete expense
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200': { description: Deleted }

  /summary:
    get:
      tags: [summary]
      summary: Monthly income/expense summary
      security:
        - bearerAuth: []
      parameters:
        - name: month
          in: query
          schema: { type: string }
      responses:
        '200': { description: Summary returned }

